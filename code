Show list of duplicated names:

import pandas as pd
df = pd.read_csv('/survey.csv')
dup = df[df.duplicated(subset=['FName','LName'], keep=False)]
print (dup)





Drop duplicated row:

dup = df[df.duplicated(subset=['FName','LName'], keep=False)]
df.drop([5], axis=0, inplace=True)
df





Add ID column starting at 1001:

df.insert(0, 'ID', range(1001, 1001 + len(df)))
df





Check data types / change data type

df.dtypes
ID        int64
FName    object
LName    object
Age       int64
Hobby    object

df.astype({'FName': 'string'}).dtypes
ID        int64
FName    string
LName    object
Age       int64
Hobby    object




Check for missing data:


